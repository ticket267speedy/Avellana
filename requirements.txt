# Dependencias del MVP para arranque local y entrega del repositorio.
#
# Este archivo debe reflejar el entorno ejecutable real del proyecto. No es solo
# para Streamlit: incluye la API, validaciones, OCR local y utilidades de prueba.
# Mantenerlo sincronizado con `pyproject.toml` reduce fricción para seniors
# developers y para cualquier entrega en entorno nuevo.

# Base del producto
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2.0,<3
PyYAML>=6.0,<7
SQLAlchemy>=2.0,<3
Jinja2>=3.1,<4
qrcode[pil]>=7.4,<9

# Frontend local / visualizacion
streamlit>=1.35,<2
pandas>=2.2,<4
reportlab>=4.0,<5

# OCR / seguridad / interoperabilidad
argon2-cffi>=23.1,<25
fhir.resources>=7.1,<8

# Desarrollo y pruebas
pytest>=8.0,<9
mypy>=1.8,<2
Faker>=25.0,<30
httpx>=0.27,<1
